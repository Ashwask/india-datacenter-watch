{
  "name": "india-dc-watch-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker backend for India Datacenter Watch — community report ingestion + photos.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
