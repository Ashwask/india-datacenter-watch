// India Datacenter Watch — runtime config.
//
// Reports are submitted as GitHub Issues (static, works on GitHub Pages — no
// backend). Set the repo the "Submit on GitHub" button files into:
window.DC_GITHUB_REPO = "Ashwask/india-datacenter-watch";

// Optional: if you ever deploy a backend (see /worker), put its URL here to also
// enable live photo uploads and approved community pins. Leave empty otherwise.
window.DC_WATCH_API = "";
