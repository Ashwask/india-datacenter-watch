// India Datacenter Watch — runtime config.
// After deploying the Cloudflare Worker (see /worker), put its URL here to make
// the report form submit live, enable photo uploads, load the Photos gallery,
// and render approved community reports as map pins. Leave empty for static mode
// (the form falls back to email / JSON download).
window.DC_WATCH_API = "";
